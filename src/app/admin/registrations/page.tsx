import { RegistrationsManager } from "@/components/admin/registrations-manager";

export const metadata = { title: "Заявки — Кабинет" };

export default function AdminRegistrationsPage() {
  return <RegistrationsManager />;
}
