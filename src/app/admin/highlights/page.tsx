import { HighlightsManager } from "@/components/admin/highlights-manager";

export const metadata = { title: "Хайлайты — Кабинет" };

export default function AdminHighlightsPage() {
  return <HighlightsManager />;
}
